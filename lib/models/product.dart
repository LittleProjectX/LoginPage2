class Product {
  String id, nama, gaji;
  DateTime createdAt, updateAt;

  Product(
      {required this.id,
      required this.nama,
      required this.gaji,
      required this.createdAt,
      required this.updateAt});
}
